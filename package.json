{
  "name": "opbattle",
  "version": "1.0.0",
  "description": "Global Esports Tournament Platform",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install && npx nest build && cd ../frontend && npm install && npm run build",
    "build:backend": "cd backend && npm install && npx nest build",
    "build:frontend": "cd frontend && npm install && npm run build",
    "start": "cd backend && node dist/main",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run start:dev",
    "dev:frontend": "cd frontend && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
